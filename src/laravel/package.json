{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "spell-check": "cspell lint -c ./cspell.json --no-progress --show-suggestions \"**/*.php\""
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "cspell": "^7.0.0",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
